// import db from "../models"; // Certifique-se de que o caminho está correto

// async function checkData() {
//   try {
//     const users = await db.User.findAll();
//     const freelancers = await db.Freelancer.findAll();
//     const categories = await db.Category.findAll();
//     const agencies = await db.Agency.findAll();

//     console.log("📌 Usuários cadastrados:", users.map(u => u.toJSON()));
//     console.log("📌 Freelancers cadastrados:", freelancers.map(f => f.toJSON()));
//     console.log("📌 Categorias cadastradas:", categories.map(c => c.toJSON()));
//     console.log("📌 Agências cadastradas:", agencies.map(a => a.toJSON()));
//   } catch (error) {
//     console.error("❌ Erro ao buscar os dados:", error);
//   }
// }

// checkData();
